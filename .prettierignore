node_modules
dist
out
coverage
bun.lock
*.tgz

# Test fixtures / golden files are byte-for-byte recordings of real API
# responses and rendered output. Prettier must not reformat them — doing so
# rewrites the recorded content (e.g. collapsing arrays) and breaks the exact
# comparison the golden tests rely on. Refresh them with RECORD=1 instead.
__fixtures__

*.snap
src/assets
